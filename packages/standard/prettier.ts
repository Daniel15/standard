import type {Config} from 'prettier';

export const standardPrettierConfig: Config = {
  arrowParens: 'avoid',
  bracketSpacing: false,
  printWidth: 100,
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
};
